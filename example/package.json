{
  "name": "example-menubar-app",
  "version": "1.0.0",
  "description": "example menubar electron app",
  "dependencies": {
    "menubar-extra": "*"
  },
  "devDependencies": {
    "electron-packager": "^5.1.0",
    "electron-prebuilt": "^0.37.8"
  },
  "main": "main.js",
  "scripts": {
    "build": "electron-packager . Example --platform=darwin --arch=x64 --version=0.37.8 --icon=Icon.icns",
    "start": "electron ."
  }
}
